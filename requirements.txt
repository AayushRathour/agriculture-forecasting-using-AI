# Django Framework
Django>=4.2,<5.0

# Image Processing (for crop image uploads and disease detection)
Pillow>=10.0.0

# Essential for Replit deployment
gunicorn>=21.2.0
whitenoise>=6.6.0

# Data Processing & Analysis
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning (for yield prediction and disease detection)
scikit-learn>=1.3.0

# Production Server
gunicorn>=21.2.0  # WSGI HTTP Server for production
whitenoise>=6.6.0  # Static file serving for production

# Database (for production)
psycopg2-binary>=2.9.9  # PostgreSQL adapter (optional)

# Caching (for production)
redis>=5.0.0  # Redis client (optional)
django-redis>=5.4.0  # Django Redis cache backend (optional)

# Environment Management
python-decouple>=3.8  # For managing environment variables (optional)

# Future Dependencies (uncomment when needed)
# tensorflow>=2.13.0  # For deep learning disease detection model
# opencv-python>=4.8.0  # For advanced image processing
# requests>=2.31.0  # For weather API integration
